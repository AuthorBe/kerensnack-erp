name: Auto Deploy ERP to cPanel

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          port: ${{ secrets.SSH_PORT }}
          script_stop: true
          script: |
            cd /home/kerensna/kerensnack-erp

            # 1. Reset perubahan lokal sebelum pull
            git reset --hard HEAD

            # 2. Tarik update kode terbaru
            git pull origin main

            # 3. Hapus berkas non-produksi dari direktori publik server
            rm -f README.md AGENTS.md *.sql

            # 4. Jalankan composer install menggunakan binary php aktif
            php ~/bin/composer install --no-dev --optimize-autoloader --no-interaction