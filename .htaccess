<IfModule mod_rewrite.c>
    RewriteEngine On

    # Protect sensitive files & folders
    RewriteRule ^(\.env|\.git|config|app|views|database|docs|test_db\.php) - [F,L,NC]

    # Forward all traffic to the /public directory
    RewriteRule ^$ public/ [L]
    RewriteRule ^((?!public/).*)$ public/$1 [L]
</IfModule>
